import React, { PureComponent } from 'react';

export default class Login extends PureComponent {
  render() {
    return (
      <div>
        <h2>请登录：</h2>
        {/* <button onClick={e => this.Login()}>点击即可登录</button> */}
      </div>
    );
  }
  Login() {
    // console.log('a');
    this.setState({

    })
  }
}
